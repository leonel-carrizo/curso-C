#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*Operadores aritméticos de tipo númerico*/

    // Suma -> operador +
    // Resta ->operador -
    // Multiplicacion -> operador *
    // Division -> Operador /
    // Resto -> operador módulo %


    /*Operadores relacionales: sirven para realizar comparaciones, aon de tipo boolean y devuelven 1 cuando es true y 0 cuando es false*/

    // Igual -> ==
    // Distinto -> !=
    // Mayor -> >
    // Mayor o Igual -> >=
    // Menor -> <
    // Menor o Igual -> <=


    /* Operadores lógicos: sirven para concatenar diferentes comparaciones*/

    // || (o) -> OR devuelve 1 (true) si alguna de las condiciones de cumple
    // && (y) -> AND devuelve 1 (true)si todas las condiciones se cumplen, sean false o true.
    // not (no) ->  NOT Invierte el resultado de la condición.


    /* Operadores de Asignación: Sirven para asignar valores a las variables*/

    // = -> Asigna valor a la variable.
    // += -> Suma el valor a variable y asigna
    // -= -> Resta valor a la variable y asigna
    // *= -> Multiplica el valor a la variable y asigna.
    // /= -> Divide le valor a la variable y asigna.

}
